---
framework: mocha
tier: small
postMerge:
  pattern:
    - "./test/test.ts"
preMerge:
  pattern:
    - "./test/test.ts"
preRun:
     command:
        - npm install
        - npm run compile
postRun:
     command:
        - node --version
version: 1.0
